import styles from "styles.module.css";

export default function Epic() {
  return (
    <div className={`${styles.centro} ${styles.fonte}`}>
      <h1>Página dedicada aos fãs de EPIC</h1>
      <p>And hunger is so heavy (hunger is so heavy)</p>
    </div>
  );
}
